# C-b is not acceptable -- Vim uses it
set-option -g prefix C-a
bind-key C-a last-window

set-option -g default-shell /usr/bin/fish
# Start numbering at 1
set -g base-index 1

# for neovim
set -g default-terminal "screen-256color"

# Allows for faster key repetition
set -s escape-time 0

# Set status bar
set -g status-bg black
set -g status-fg "#ffca28"
set -g status-left-fg "#ff6600"
set -g status-right "#[fg=yellow]#H"
set -g default-terminal "screen-256color"

# set -g set-titles on
# set -g set-titles-string "#T"

# Rather than constraining window size to the maximum size of any client 
# connected to the *session*, constrain window size to the maximum size of any 
# client connected to *that window*. Much more reasonable.
setw -g aggressive-resize on

# Allows us to use C-a a <command> to send commands to a TMUX session inside 
# another TMUX session
bind-key a send-prefix

# Activity monitoring
#setw -g monitor-activity on
#set -g visual-activity on

# Example of using a shell command in the status line
#set -g status-right "#[fg=yellow]#(uptime | cut -d ',' -f 2-)"

# Highlight active window
set-window-option -g window-status-current-bg "#FFAF00"
set-window-option -g window-status-current-fg "#555555"

# Session name in status bar
set-window-option -g status-left " #S "

set-window-option -g mode-keys vi

bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"
